@model IEnumerable<eWallet.Models.FarmerGrant>

@{
    ViewBag.Title = "My Grants";
}

<div class="">
    <div class="page-title">
        <div class="title_left">
            <h3>My Grants</h3>
        </div>
        <div class="title_right">
            <div class="col-md-5 col-sm-5 col-xs-12 form-group pull-right top_search">
                <div class="input-group">
                    <span class="input-group-btn">
                        <a class="btn btn-default pull-right" href="@Url.Action("OpenGrants")">
                            <i class="fa fa-plus"></i> Apply for Grant
                        </a>
                    </span>
                </div>
            </div>
        </div>
    </div>
    <div class="clearfix"></div>
    <div class="row">
        <div class="col-md-12 col-sm-12 col-xs-12">
            <div class="x_panel">
                <div class="x_title">
                    <div class="clearfix"></div>
                </div>
                <div class="x_content">
                    <table id="datatable" class="table table-striped table-bordered">
                        <thead>
                            <tr>
                                <th>
                                    Application @Html.DisplayNameFor(model => model.Status)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.Grant.Title)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.Grant.MonetaryWorth)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.Grant.Status)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.ApprovedAmount)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.AvailableAmount)
                                </th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Status)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Grant.Title)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Grant.MonetaryWorth)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Grant.Status)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.ApprovedAmount)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.AvailableAmount)
                                    </td>
                                    <td>
                                        <a href="@Url.Action("Index","MyProducts", new {GrantId = item.GrantId})" class='btn btn-primary'>
                                            <i class='fa fa-pencil'></i> Visit Shop
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
@section Styles{
    @Styles.Render("~/Content/datatable")
}
@section Scripts {
    @Scripts.Render("~/vendors/datatable")
    <script>
        $(document).ready(function() {
            $('#datatable').dataTable();
        });
    </script>
}